# pnpm settings for better compatibility with Windows-mounted workspaces (9p/DrvFS)
# and to avoid placing the pnpm store inside the repo.
store-dir=/home/node/.pnpm-store
node-linker=hoisted
package-import-method=copy
